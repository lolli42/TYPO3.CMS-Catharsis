class_name: AcceptanceTester
modules:
    enabled:
        - WebDriver
        - \Helper\Acceptance
        - Asserts
        - \Helper\Unit
    config:
      WebDriver:
        url: http://127.0.0.1:8000
        browser: phantomjs

env:
    firefox:
        modules:
            config:
                WebDriver:
                    browser: 'firefox'
                    window_size: 1024x768
                    http_proxy: direct
    phantom:
        # nothing changed